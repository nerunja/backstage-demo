import { createRouteRef } from '@backstage/frontend-plugin-api';

// Route reference for the Agentic Lab page. Other plugins can use this
// (via the plugin's `routes` export) to link to the page.
export const rootRouteRef = createRouteRef();
