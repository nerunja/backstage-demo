# Python agents for the Agentic Lab (ADK + A2A + AG-UI)
# NOTE: agents/ is uv-managed (pyproject.toml); this file is kept only as a
# pip fallback. a2a-sdk must stay <1.0.0 — 1.x is a protobuf rewrite whose
# module layout (a2a.server.routes etc.) is incompatible with our agents.
google-adk>=1.0.0
ag-ui-adk>=0.7.0
a2a-sdk>=0.2.0,<1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
litellm>=1.40.0
